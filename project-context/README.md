# Project context

Full project context for the Cloud Title website redesign, mirrored here so this
repo (`cloudtitle-demo`) is self-contained for anyone working on it.

Source of truth: the docs repo **`cloud-title-website`** (`Calldentalai/cloud-title-website`).
If anything here is edited, sync the change back to that repo too.

> **Note:** the `CLAUDE.md` in *this* folder is the **project/docs** rules.
> The `CLAUDE.md` at the repo root is the **demo-app** build rules — that one governs work in this repo.

## Contents

- `CLAUDE.md` — project hard rules + folder map (docs repo version)
- `AGREED-SCOPE.md` — what we're building, page list, deadlines
- `TEAM-ONBOARDING.md` — full walkthrough for a new team member
- `briefs-from-thomas/` — the 3 source documents from the client
- `proposal-sent/` — original proposal (background only)
- `response-from-thomas/` — client's written scope confirmation
- `tooling/divi-mcp-setup.md` — Divi 5 build toolchain setup
